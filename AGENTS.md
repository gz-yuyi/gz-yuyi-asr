Write user-facing documentation content in Chinese unless a file already has a clearly established different language.

Keep `docs/` limited to externally observable client contracts: requests, responses, errors, protocol ordering, compatibility, and behavior clients may rely on. Do not document server architecture, source layout, models or algorithms, storage/database paths, environment variables, deployment, operations, or debugging internals here; move those details to the implementation repository.

